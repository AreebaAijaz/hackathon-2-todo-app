name: create auth-integration

description: "Integrates Better Auth with JWT verification across frontend and backend"

Skill Logic: "Configures Better Auth with JWT plugin, implements signup/login flows, creates JWT verification middleware for FastAPI, handles token refresh, and ensures user isolation in multi-tenant applications."