[project]
name = "recurring-service"
version = "1.0.0"
description = "Recurring task auto-creation service — consumes task.completed events"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "httpx>=0.27",
]
