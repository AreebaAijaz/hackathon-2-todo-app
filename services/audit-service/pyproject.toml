[project]
name = "audit-service"
version = "1.0.0"
description = "Task event audit trail service — persists all task events to audit_log table"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "psycopg[binary]>=3.3.2",
    "sqlmodel>=0.0.31",
]
