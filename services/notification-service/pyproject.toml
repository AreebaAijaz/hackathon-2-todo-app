[project]
name = "notification-service"
version = "1.0.0"
description = "Task reminder notification service — logs reminders before due dates"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "httpx>=0.27",
]
