apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: audit-task-events
  namespace: default
spec:
  pubsubname: kafka-pubsub
  topic: task-events
  route: /task-events
scopes:
  - audit-service
